{
  "name": "Payment",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string",
      "description": "Paying user id"
    },
    "userEmail": {
      "type": "string"
    },
    "planId": {
      "type": "string",
      "description": "Plan purchased (free, pro, agency), if a subscription plan"
    },
    "productId": {
      "type": "string",
      "description": "Product id, if a one-time product purchase"
    },
    "promptSessionId": {
      "type": "string",
      "description": "PromptGeneratorSession id, if a prompt pack unlock purchase"
    },
    "itemName": {
      "type": "string",
      "description": "Human-readable name of what was purchased"
    },
    "amountCents": {
      "type": "number"
    },
    "currency": {
      "type": "string",
      "default": "USD"
    },
    "squarePaymentId": {
      "type": "string"
    },
    "squareReceiptUrl": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "completed",
        "pending",
        "failed"
      ],
      "default": "pending"
    },
    "errorMessage": {
      "type": "string"
    }
  },
  "required": [
    "userId"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.userId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "write": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}