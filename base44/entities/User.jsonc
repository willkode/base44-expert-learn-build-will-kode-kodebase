{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user",
      "description": "Application role"
    },
    "plan": {
      "type": "string",
      "enum": [
        "free",
        "pro",
        "agency"
      ],
      "default": "free",
      "description": "Subscription plan"
    }
  },
  "required": []
}